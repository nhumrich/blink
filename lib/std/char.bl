// TryFrom[Int] for Char — fallible Int -> Char conversion.
// Spec §03c_protocols.md and decisions/char-conversions.md (Q2).

@module("")

impl TryFrom[Int] for Char {
    fn try_from(value: Int) -> Result[Char, ConversionError] {
        Char.from_code_point(value)
    }
}
